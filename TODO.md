# TODO

- Audit Git author identity in each pending local project before its first public push; the profile repo and `briefboard-local` now use the documented no-reply GitHub identity.
- Push local `deploy-gate` readiness commit `d7344ee` from `/Users/manuelsampedro/Documents/Codex/2026-05-24/flagships/deploy-gate` after GitHub write auth is available; current HTTPS push fails with `could not read Username for 'https://github.com': Device not configured`, and direct SSH push fails with `Permission denied (publickey)`.
- Continue publishing follow-up lab notes or recipes directly from flagship repo changes; `briefboard-local`, `agent-run-ledger`, `repo-flightcheck`, `verify-by-change`, and `codex-review-packet` now have behavior-changing follow-ups with public evidence.
- Apply `recipes/real-diff-to-recipe.md` to future behavior-changing flagship repo diffs; `agent-diff-splitter` public launch, `agent-claim-check` public launch, `agent-review-map` public launch, `agent-diff-budget` public launch, `agent-decision-guard` public launch, `agent-eval-runner` public launch, `profile-proof-audit` public launch, `agent-publish-queue` public launch, `agent-proof-packet` public launch, `agent-merge-readiness` public launch, `agent-change-risk` public launch, `agent-closeout-check` public launch, `agent-scope-guard` public launch, `runbook-drift-check` public launch, `agent-rollback-plan` public launch, `agent-ci-failure-packet` public launch, `agent-secret-sentinel` public launch, `diff-to-eval` public launch, `agent-task-contract` public launch, `agent-run-ledger` readiness task-contract import, `repo-flightcheck` structured task-contract metadata, `codex-review-packet` task-contract envelope summaries, `agent-run-ledger` task-contract envelope import, `verify-by-change` task-contract envelope metadata, `agent-run-ledger` task contract evidence import, `codex-review-packet` task contract sections, `repo-flightcheck` task contract validation, `repo-flightcheck` remote failure classification, `agent-run-ledger` published-HEAD proof import, `codex-review-packet` published-HEAD proof, `repo-flightcheck` published-HEAD readiness, `repo-flightcheck` remote readiness, `briefboard-local` handoff scope warnings, `agent-run-ledger` sensitive review packet import, `codex-review-packet` sensitive change checks, `verify-by-change` sensitive path guidance, `repo-flightcheck` local tool availability, `agent-run-ledger` rendered verification envelope import, `codex-review-packet` generated verification envelopes, `verify-by-change` Python CLI context, `repo-flightcheck` Python CLI entrypoint readiness, `agent-run-ledger` review packet CI evidence import, `codex-review-packet` CI evidence rendering, `agent-run-ledger` GitHub Actions run evidence import, `repo-flightcheck` Node CLI entrypoint readiness, `verify-by-change` Node CLI context, `agent-run-ledger` review packet readiness import, `agent-run-ledger` envelope readiness evidence, `verify-by-change` packet readiness context, `codex-review-packet` readiness contract rendering, `agent-run-ledger` readiness contract import, `repo-flightcheck` agent readiness contracts, `agent-run-ledger` packet verification import, `codex-review-packet` verification envelope rendering, `verify-by-change` review packet source support, `agent-run-ledger` review packet import, `codex-review-packet` generated verification checklists, `verify-by-change` GitHub Action guidance, `repo-flightcheck` GitHub Action detection, `agent-run-ledger` CI parity, `briefboard-local` repo readiness contract, `verify-by-change` repo readiness contract, `agent-run-ledger` repo readiness import, `codex-review-packet` repo readiness reports, `repo-flightcheck` Python unittest detection, `agent-run-ledger` JSON envelope import, `verify-by-change` JSON envelopes, `repo-flightcheck` documented command checks, `codex-review-packet` review maps, `briefboard-local` readiness checks, `agent-run-ledger` strict doctor gates, `verify-by-change` base-plus-working-tree coverage, `repo-flightcheck` CI verification coverage, `codex-review-packet` verification checklist packets, `agent-run-ledger` checklist import, `verify-by-change` empty-diff guard, `codex-review-packet` bounded diff packets, `agent-run-ledger` JSON doctor output, `verify-by-change` working-tree coverage, `briefboard-local` JSON restore, and `repo-flightcheck` dirty-tree detection are current quality bars.
- Let the weekly quality audit run once under the new client-credibility rules, then tighten anything that still feels too meta.
- Decide whether `scripts/commit_daily_update.sh` should warn when non-staged scratch files remain after a successful publish run.
- Audit any external lab or recipe automation wrappers to make sure they pass exact artifact paths into `scripts/commit_daily_update.sh`.
