# TODO

- Audit Git author identity in each pending local project before its first public push; the profile repo and `briefboard-local` now use the documented no-reply GitHub identity.
- Continue publishing follow-up lab notes or recipes directly from flagship repo changes; `briefboard-local`, `agent-run-ledger`, `repo-flightcheck`, `verify-by-change`, and `codex-review-packet` now have behavior-changing follow-ups with public evidence.
- Apply `recipes/real-diff-to-recipe.md` to future behavior-changing flagship repo diffs only after public proof exists. Current quality bars include pre-run context (`agent-task-contract`, `agent-repo-map`, `agent-handoff-brief`, `agent-instruction-audit`, `repo-flightcheck`), scope and review gates (`agent-scope-guard`, `agent-worktree-guard`, `agent-diff-budget`, `agent-diff-splitter`, `agent-review-map`), evidence and closeout gates (`agent-proof-packet`, `agent-command-receipt`, `agent-claim-check`, `agent-closeout-check`, `agent-run-ledger`), evaluation loops (`diff-to-eval`, `agent-eval-runner`), and safety or operations tools (`agent-secret-sentinel`, `agent-ci-failure-packet`, `agent-rollback-plan`, `runbook-drift-check`, `agent-publish-queue`, `profile-proof-audit`). Each promotion should have tests, CI, public URLs, a lab note, and a reusable recipe or explicit reason not to add one.
- Keep GitHub SSH publishing durable for future public proof repos; this run restored terminal auth by loading `~/.ssh/id_ed25519_github_codex` into `ssh-agent`, after earlier HTTPS pushes failed with `could not read Username for 'https://github.com': Device not configured` and SSH pushes failed with `Permission denied (publickey)`.
- Let the weekly quality audit run once under the new client-credibility rules, then tighten anything that still feels too meta.
- Decide whether `scripts/commit_daily_update.sh` should warn when non-staged scratch files remain after a successful publish run.
- Audit any external lab or recipe automation wrappers to make sure they pass exact artifact paths into `scripts/commit_daily_update.sh`.
